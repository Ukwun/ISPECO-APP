{
  "name": "ispeco-socket-server",
  "version": "1.0.0",
  "main": "dist/server/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "ts-node server/index.ts",
    "build": "tsc",
    "postbuild": "node -e \"console.log('✅ build complete - listing dist:'); const f=require('fs'); const p='dist'; try{const walk=(d,pre='')=>{f.readdirSync(d).forEach(fn=>{const fp=require('path').join(d,fn); if(f.statSync(fp).isDirectory()) walk(fp,pre+fn+'/'); else console.log(pre+fn)});} ; if(f.existsSync(p)) walk(p); }catch(e){console.error(e)}\"",
  "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "@types/express": "^4.17.21",
    "@types/node": "^18.0.0"
  }
}
